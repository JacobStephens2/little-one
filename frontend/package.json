{
  "name": "baby-tracker-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript frontend for baby.stephens.page — bundled to ../public/app.js",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}

# Secrets — never commit
server/.env
*.env
*.pem

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / frontend build tooling (public/app.js IS committed — it's the served artifact)
node_modules/
frontend/node_modules/

# OS / editor
.DS_Store
*.swp
.vscode/
